import PageRootComponent from "./PageRootComponent";
import { memo } from "react";

const PageNewCollectionCenter = async () => {
  return <PageRootComponent />;
};

export default memo(PageNewCollectionCenter);
