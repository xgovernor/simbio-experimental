import PageRootComponent from "./PageRootComponent";
import { memo } from "react";

const PageSingleCollectionCenter = async () => {
  return <PageRootComponent />;
};

export default memo(PageSingleCollectionCenter);
