import Layout from "@/components/ui/AppLayout";
import { memo } from "react";

const PageRootComponent = () => {
  return (
    <Layout>
      <h1>Support Chat box with Chat session list</h1>
    </Layout>
  );
};

export default memo(PageRootComponent);
