import { type Config } from 'prettier';

const config: Config = {
    trailingComma: 'none',
    tabWidth: 4,
    singleQuote: true,
    jsxSingleQuote: false,
    endOfLine: 'lf',
    semi: true,
    useTabs: false,
    printWidth: 120
};

export default config;
